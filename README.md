# Chatty
A kinda full fledged chat message app.
  
## Tech Stack:
- Frontend (Client): **Solid JS**
- Backend (Server): **Go** (Golang)
- Database: **Postgres**

I push the `.env` file in `server` because this is a side project and this project is meant to run locally so no harm in putting it here.
